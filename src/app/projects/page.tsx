import { ProjectsPage } from "@portfolio/components";
import React from "react";

const Projects = () => {
    return (
        <div className="w-full min-h-screen p-10 bg-primary">
            <ProjectsPage />
        </div>
    );
};

export default Projects;
