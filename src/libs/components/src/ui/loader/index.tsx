import React from "react";

const Loader = () => {
    return (
        <div className="animate-spin transition-all duration-500 h-44 w-44">
            Loading...
        </div>
    );
};

export default Loader;
